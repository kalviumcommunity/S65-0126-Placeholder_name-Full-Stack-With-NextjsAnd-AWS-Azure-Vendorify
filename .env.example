# .env.example - Template for required environment variables
# ⚠️ DO NOT COMMIT REAL SECRETS TO THIS FILE!
# This file is a template. Copy to .env.local for local development.

# ============================================================================
# 🐳 DOCKER COMPOSE SETUP (Local Development)
# ============================================================================
# When using docker compose up --build, these values are automatically set
# in docker-compose.yml. This file is for LOCAL development (npm run dev).

# Next.js Configuration
NODE_ENV=development
PORT=3000

# ============================================================================
# 📊 DATABASE CONFIGURATION
# ============================================================================

# For LOCAL Development (npm run dev):
DATABASE_URL="postgresql://vendorify_user:vendorify_password@localhost:5432/vendorify_db?schema=public"

# For DOCKER Compose (automatically set in docker-compose.yml):
# DATABASE_URL="postgresql://vendorify_user:vendorify_password@postgres:5432/vendorify_db?schema=public"

# For STAGING (use your staging database):
# DATABASE_URL="postgresql://<user>:<password>@staging-db.example.com:5432/vendorify_staging?schema=public"

# For PRODUCTION (use your production database):
# DATABASE_URL="postgresql://<user>:<password>@prod-db.example.com:5432/vendorify_prod?schema=public"

# ============================================================================
# 🗂️ REDIS CONFIGURATION
# ============================================================================

# For LOCAL Development (npm run dev - if you have Redis running):
REDIS_URL="redis://localhost:6379"

# For DOCKER Compose (automatically set in docker-compose.yml):
# REDIS_URL="redis://redis:6379"

# For PRODUCTION (managed service):
# REDIS_URL="rediss://<user>:<password>@redis-cluster.example.com:6379"

# ============================================================================
# 🌐 PUBLIC API CONFIGURATION (Exposed to Browser)
# ============================================================================

# Client-side base URL - must start with NEXT_PUBLIC_
# This is available in browser code
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000"

# Application environment - useful for feature flags
NEXT_PUBLIC_APP_ENV="development"

# ============================================================================
# 🔐 SECRET KEYS (Server-side Only)
# ============================================================================
# ⚠️ NEVER expose these to the browser
# ⚠️ NEVER commit real values to git
# Store real secrets in: GitHub Secrets → GitHub Actions, AWS Parameter Store, Docker secrets

# JWT Secret (if using authentication)
JWT_SECRET="__REPLACE_ME_WITH_RANDOM_STRING_IN_PRODUCTION__"

# API Keys (external services)
EXTERNAL_API_KEY="__REPLACE_ME__"

# ============================================================================
# 📝 USAGE INSTRUCTIONS
# ============================================================================

# For LOCAL DEVELOPMENT (npm run dev):
# 1. Copy this file: cp .env.example .env.local
# 2. Update DATABASE_URL to point to your local PostgreSQL
# 3. Update REDIS_URL to point to your local Redis
# 4. Run: npm run dev

# For DOCKER COMPOSE DEVELOPMENT (docker compose up):
# 1. The docker-compose.yml automatically provides:
#    - DATABASE_URL pointing to postgres service
#    - REDIS_URL pointing to redis service
# 2. No need to create .env.local - docker-compose.yml handles it
# 3. Run: docker compose up --build

# For PRODUCTION:
# 1. Set secrets via GitHub Actions or AWS Parameter Store
# 2. Do NOT create .env files with production secrets
# 3. Use environment variable injection at deploy time

# ============================================================================
# 📚 REFERENCE: What Each Variable Does
# ============================================================================

# NODE_ENV
# - Values: development, staging, production
# - Controls: Next.js optimization, logging, error details
# - Visibility: Server-side only
# - In Docker: Already set to "development" in docker-compose.yml

# DATABASE_URL
# - Format: postgresql://[user]:[password]@[host]:[port]/[database]
# - Visibility: Server-side ONLY
# - Used by: ORM libraries, database clients
# - In Docker: postgres service provides this

# REDIS_URL
# - Format: redis://[host]:[port] or redis://[password]@[host]:[port]
# - Visibility: Server-side ONLY
# - Used by: Session store, caching
# - In Docker: redis service provides this

# NEXT_PUBLIC_API_BASE_URL
# - Visibility: CLIENT-SIDE (exposed to browser)
# - Must start with NEXT_PUBLIC_ prefix
# - Used by: fetch() calls in browser
# - Example: http://localhost:3000 (local), https://api.example.com (production)
