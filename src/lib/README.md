Utility functions, API clients, feature flags, and shared configuration.

Guidelines:
- Keep pure functions here for easy unit testing.
- Organize by capability: `api/`, `formatters/`, `validators/`.

Example:
```
src/lib/api/client.ts
src/lib/formatters/formatDate.ts
```
